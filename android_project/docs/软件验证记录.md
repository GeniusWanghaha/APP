# 软件验证记录（2026-04-01）

## 1. Android 构建与测试
- `./gradlew testDemoDebugUnitTest testContestDebugUnitTest lintDemoDebug lintContestDebug assembleDemoDebug assembleContestDebug assembleContestRelease` 通过

## 2. 模拟器联调（软件侧虚拟验证）
- 检测到 AVD：`Medium_Phone_API_36.1`
- 安装 APK：`app/build/outputs/apk/demo/debug/app-demo-debug.apk`（测试后已清理，可由 `assembleDemoDebug` 重新生成）
- 启动 Activity：`com.photosentinel.health.MainActivity`
- `adb shell ps -A | grep com.photosentinel.health.demo.debug` 返回有效进程（应用成功拉起）
- 截图证据：`docs/logs/photosentinel_emulator_20260401_202618.png`

## 3. ESP32-S3 工具链安装（项目目录外）
- 安装方式：`winget install Espressif.EIM-CLI` + `eim install ...`
- 安装路径：`D:\Espressif`
- 验证：`idf.py --version` 返回 `ESP-IDF v5.2.2`

## 4. 固件编译与烧录验证
- 工程：`D:\optoelectronic_design\APP\OPD`
- 构建命令：`idf.py set-target esp32s3`、`idf.py build` 通过
- 烧录命令：`idf.py -p COM3 flash` 通过
- 启动日志：`docs/logs/esp32_boot_20260401_201827.log`（可见 BLE 广播启动与自检通过）

## 5. 真机 BLE 联调验证（非人体场景）
- 联调脚本：`tools/ble_hw_smoke_test.py`
- 验证内容：
  - 扫描连接设备
  - `START/STOP`
  - `SET_CFG`（LED PA、PPG phase、PPG latency、温度开关、日志级别、PPG 模式）
  - `GET_INFO`（读 control/status 快照）
  - `SELF_TEST`（读取 `self_test_fail_bitmap=0`）
  - `SYNC_MARK`（在数据帧检测到 `state_flags bit9`）
- 证据日志：
  - `docs/logs/hardware_smoke_20260401_202342.log`
  - `docs/logs/hardware_demo_35s_20260401_201530.log`（35 秒演示流）

## 6. 当前仍待验证
- 工程模式 UI 的完整自检结果与错误码展示（需真机 APP 页面触发）
- PPG 固定延时标定闭环（需真实人体/场景配合）
- 功耗与热管理实测（需长时实机）
- 手机 ADB 真机链路（2026-04-01 20:26 检查结果：Windows 仅识别为 MTP/WPD，`adb devices` 暂无真机）

## 7. 空间优化动作
- Gradle 用户缓存已迁移到项目外：`D:\GradleUserHome`
- `android_project/.gradle`、`android_project/app/build`、`OPD/build` 已清理
- 安装包已归档到 `dist/packages`
